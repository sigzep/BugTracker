@model IEnumerable<BugTracker.Models.RolesViewModel>

@{
    ViewBag.Title = "ListRoles";
}

<h2>List Roles</h2>

<p>
    <a href="@Url.Action("CreateRoles")" style="color:green; font-weight: 800">CREATE NEW</a>
   @*  @Html.ActionLink("Create New", "CreateRoles")  *@
</p>
<table class="table">
    <tr>
        <th style="color: black; background-color:#F0F0F0; font-size: 20px;">
            @Html.DisplayNameFor(model => model.RoleName)
        </th>
        <th style="background-color:#F0F0F0"></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RoleName)
        </td>
        <td>
            <a href="@Url.Action("EditRoles",new { id=item.RoleId })" style="color:green; font-weight: 800">EDIT</a>
            @*  @Html.ActionLink("Edit", "Edit", new { id = item.Id })  @(" | ") *@
            @* <a href="@Url.Action("Details",new { id=item.RoleId })" style="color:blue; font-weight: 800">DETAILS</a> *@
            @* @Html.ActionLink("Details", "Details", new { id = item.Id })  @(" | ") *@
         </td>
    </tr>
}

</table>
